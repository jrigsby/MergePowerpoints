<UserControl x:Class="PDFTools.MergePdfsControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PDFTools"
             xmlns:vm="clr-namespace:PDFTools.Models"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Toast Notification Overlay -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ItemsControl Grid.Row="1" Grid.RowSpan="1" ItemsSource="{Binding ElementName=Root, Path=Toasts}" VerticalAlignment="Top"
                      HorizontalAlignment="Right" Margin="0,10,20,0" Panel.ZIndex="1000">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ToastViewModel}">
                    <Border Background="#FFFFA07A" BorderBrush="DarkRed" BorderThickness="1" CornerRadius="5"
                            Margin="0,0,0,10" Padding="10" Width="300">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.5" />
                        </Border.Effect>
                        <DockPanel>
                            <Button DockPanel.Dock="Right" Content="X" Command="{Binding CloseCommand}"
                                    Width="20" Height="20" Margin="5,0,0,0" VerticalAlignment="Top"
                                    Background="Transparent" BorderThickness="0" FontWeight="Bold" Foreground="DarkRed" />
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="Black" FontSize="12"
                                       VerticalAlignment="Center" />
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- PDF File Selection -->
            <TextBlock Text="PDF Files:" VerticalAlignment="Center" Margin="5" />
            <Button Content="Browse..." Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" Margin="5" Padding="10,2"
                    Click="BrowseFile_Click" Width="100" />

            <!-- Output Folder Selection -->
            <TextBlock Text="Output Folder:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5" />
            <TextBox x:Name="OutputFolderTextBox" Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center" />
            <Button Content="Browse..." Grid.Row="1" Grid.Column="2" Margin="5" Padding="10,2"
                    Click="BrowseFolder_Click" />

            <!-- Output Filename Format -->
            <TextBlock Text="Output filename:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5" />
            <TextBox x:Name="OutputFilenameTextBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="5"
                     VerticalAlignment="Center" Text="" />

            <!-- Split Button -->
            <StackPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" VerticalAlignment="Center"  Orientation="Horizontal">
            <Button Content="PDFSharp Merge PDFs"  HorizontalAlignment="Right"
                    Margin="5" Padding="20,5" Click="MergePdfs_Click" />
            <Button Content="Syncfusion Merge PDFs"  HorizontalAlignment="Right"
                    Margin="5" Padding="20,5" Click="SyncfusionMergePdfs_Click" />
            <Button Content="Telerik Merge PDFs"  HorizontalAlignment="Right"
                    Margin="5" Padding="20,5" Click="TelerikMergePdfs_Click" />
</StackPanel>
            <!-- Results Grid -->
            <DataGrid x:Name="ResultsGrid" Grid.Row="5" Grid.ColumnSpan="3" Grid.Column="0" Margin="5" AutoGenerateColumns="False"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="Action" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Open" Click="OpenPdf_Click" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>